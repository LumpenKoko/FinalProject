let contextPath;
let loginUser;

// body onload 시 실행되는 초기 함수
function init(path, user){
    contextPath = path;
    loginUser = user;
    // drawSearchPage();
    // document.querySelector('.like-btn').click();
}